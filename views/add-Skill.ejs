<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Añadir Competencia</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .container {
            background: white;
            border: 1px solid #ddd;
            border-radius: 8px;
            width: 600px;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .header {
            background-color: #ffd700;
            padding: 10px;
            font-size: 24px;
            font-weight: bold;
            border-radius: 8px 8px 0 0;
            text-align: center;
            margin-bottom: 20px;
        }
        form {
            display: flex;
            flex-direction: column;
        }
        label {
            font-weight: bold;
            margin-bottom: 5px;
        }
        input, textarea, button {
            font-size: 16px;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }
        textarea {
            resize: vertical;
        }
        input[type="file"] {
            padding: 0;
        }
        button {
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">Añadir Competencia</div>
        <form action="/skills/add" method="POST" enctype="multipart/form-data">
            <label for="text">Nombre de la Competencia:</label>
            <input type="text" id="text" name="text" placeholder="Ejemplo: Crear un circuito básico" required>

            <label for="description">Descripción:</label>
            <textarea id="description" name="description" placeholder="Describe la competencia..." rows="3" required></textarea>

            <label for="icon">Subir Ícono:</label>
            <input type="file" id="icon" name="icon" accept="image/*">

            <label for="score">Puntuación:</label>
            <input type="number" id="score" name="score" placeholder="Ejemplo: 10" required>

            <label for="tasks">Tareas (una por línea):</label>
            <textarea id="tasks" name="tasks" placeholder="Escribe las tareas..." rows="4" required></textarea>

            <label for="resources">Recursos (uno por línea):</label>
            <textarea id="resources" name="resources" placeholder="Proporciona enlaces o recursos..." rows="4" required></textarea>

            <button type="submit">Añadir Competencia</button>
        </form>
    </div>
</body>
</html>
